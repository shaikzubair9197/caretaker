TASK: Draft one professional follow-up email about a single meeting item.

You are given a JSON object with:
- "context": the masked meeting item (title, detail, owner) — names/emails are already replaced with <...> tokens.
- "retrieval_results": a list of validated knowledge facts. Each has a "value_ref" plus "source_type" and "source_id". These are the ONLY facts you may rely on.

══ CREDENTIAL DELIVERY — READ FIRST ══
If "context" contains a "secure_refs" list, this email is a CREDENTIAL DELIVERY, not a follow-up.
You MUST:
  1. Write the email as if you are sending the credential RIGHT NOW (not asking, not checking in).
  2. Place each entry's exact "ref" string (e.g. {{SECURE_REF:1}}) verbatim in the body EXACTLY ONCE, on its own line.
  3. Do NOT write the actual value — only the placeholder. The real value is injected securely after.
  4. Do NOT omit or rename any placeholder. Every entry in secure_refs must appear in the body.

Example — if secure_refs contains one entry with ref "{{SECURE_REF:1}}" and label "production openai api_key":
{
  "subject": "Production OpenAI API Key",
  "body": "Hi <S1_SPEAKER_2>,\n\nPlease find the production openai api_key below:\n\n{{SECURE_REF:1}}\n\nLet me know if you have any questions.",
  "citations": []
}
══════════════════════════════════════

RULES (for all emails, including credential delivery):
1. Keep every <...> token EXACTLY as written. Never invent, guess, expand, or reveal a name, email, or value. Greet the ADDRESSEE using "context.recipient_token" verbatim (e.g. if recipient_token is "<S1_SPEAKER_2>", start "Hi <S1_SPEAKER_2>,"). NEVER greet "context.owner_token" — the owner is the sender who committed to the task, not the person receiving this email. If recipient_token is null, omit the name and open with "Hi,".
2. Write a short subject line and a short body (greeting, the follow-up, a brief sign-off). Plain text or simple HTML only.
3. Every factual claim MUST be backed by a fact in "retrieval_results". For each one, add an entry to "citations" with its exact "value_ref", "source_type", and "source_id".
4. Use ONLY value_refs that appear in "retrieval_results". If a claim cannot be backed by a provided value_ref, do not write that claim.
5. Never invent a {{SECURE_REF:n}} that is not in the secure_refs list.

Return ONLY a JSON object in this exact shape — no prose, no markdown:

{
  "subject": "short subject line",
  "body": "the email body, using <...> tokens verbatim",
  "citations": [
    {"value_ref": "<exact value_ref from retrieval_results>", "source_type": "...", "source_id": 0}
  ]
}

If you cannot produce a grounded draft, return: {"error": "unable to process"}

Input (JSON):
