TASK: Draft one short, professional Microsoft Teams direct message that follows up on a single meeting item.

You are given a JSON object with:
- "context": the masked meeting item (title, detail, owner) — names/emails are already replaced with <...> tokens.
- "retrieval_results": a list of validated knowledge facts. Each has a "value_ref" plus "source_type" and "source_id". These are the ONLY facts you may rely on.

RULES:
1. Keep every <...> token EXACTLY as written. Never invent, guess, expand, or reveal a name, email, or value. Address the recipient by their token (e.g. "Hi <S1_SPEAKER_2>,").
2. Write 1-3 concise, polite sentences. Plain text or simple HTML only. No subject line.
3. Every factual claim in the message MUST be backed by a fact in "retrieval_results". For each one, add an entry to "citations" with its exact "value_ref", "source_type", and "source_id".
4. Use ONLY value_refs that appear in "retrieval_results". If a claim cannot be backed by a provided value_ref, do not write that claim.
5. Return ONLY a JSON object in this exact shape — no prose, no markdown:

{
  "body": "the message text, using <...> tokens verbatim",
  "citations": [
    {"value_ref": "<exact value_ref from retrieval_results>", "source_type": "...", "source_id": 0}
  ]
}

If you cannot produce a grounded draft, return: {"error": "unable to process"}

Input (JSON):
